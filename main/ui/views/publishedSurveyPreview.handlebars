<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>{{name}} – Preview</title>
  <style>
    table { border-collapse: collapse; width: 100%; }
    th, td { border: 1px solid #ccc; padding: 6px; }
    th { background: #f5f5f5; }
  </style>
</head>
<body>

<h2>{{name}} ({{status}})</h2>
<p>
  Showing first {{rows.length}} of {{totalRows}} rows
</p>

<table>
  <thead>
    <tr>
      {{#each fields}}
        <th>{{this.label}}</th>
      {{/each}}
    </tr>
  </thead>

  <tbody>
    {{#each rows}}
      <tr>
        {{#each ../fields}}
          <td>{{lookup ../this value}}</td>
        {{/each}}
      </tr>
    {{/each}}
  </tbody>
</table>

</body>
</html>
